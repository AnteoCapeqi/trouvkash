import * as React from "react";
import Terminals from "./terminals";
import Map from "./map";

const Main = () => {
    return (
        <div>
            <Map />
        </div>
    );
};

export default Main;
